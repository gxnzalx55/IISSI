{
    "getAll": {
        "description": "Gets all entries from 'v_votes'",
        "route": "/v_votes",
        "method": "GET",
        "sql": "SELECT * FROM v_votes",
        "auth_required": false,
        "allowed_roles": [
            "*"
        ]
    }
}